# 原生版驗證清單

分兩階段：**先用 Expo Go 掃一遍**（不需 EAS 帳號、不需 build，五分鐘就能開始），
真的要上架前再用 preview binary 做完整驗收。兩份清單刻意分開——
Expo Go 驗得到的東西不必等 build，驗不到的也不該讓人誤以為驗過了。

---

## 第一階段：Expo Go（現在就能做）

```sh
npx expo start --go
```

手機與電腦連同一個網段，用相機掃終端機的 QR code（Android 用 Expo Go App 內建掃描器）。
若掃不到，在 Expo Go 手動輸入本機網址即可。

### Expo Go 驗得到

| # | 項目 | 怎麼判定過關 | 為什麼 Web 測不到 |
|---|------|------------|-----------------|
| 1 | **分享圖片有內容（A25）** | 揭曉頁按「分享」→ 選存檔或傳給自己 → **打開那張圖，確認看得到籤詩、棋子與卦象，不是全白/全黑** | `view-shot` 的 iOS 端是 `drawViewHierarchyInRect`（照螢幕上的樣子重畫），離屏元件截出空白是有回報的行為。Web 端走的是完全不同的路徑，測不到這件事 |
| 2 | **空白截圖的降級是否誤傷** | 同上，若拿到的是**文字分享而非圖片**，代表 `isPlausibleCapture` 把正常卡片誤判成空白 | 門檻 4KB 是估的，沒有實機樣本驗證過真卡片的實際大小 |
| 3 | **書法字型真的載入** | 籤詩四句應為襯線楷書風，不是系統黑體 | 原生走執行期 `loadAsync` 載入 1.2MB 子集字型，與 Web 的 Google Fonts 是兩套路徑 |
| 4 | **日文／英文字形不缺字** | 設定切 ja 與 en，看六爻面板的斷語有沒有缺字方框（Session 38 新增的譯文） | 子集字型只收錄掃描到的字元，漏收在 Web 上看不出來（瀏覽器有系統字型後備） |
| 4b | **《靈棋經》原典字形不缺字** | 占卜圖鑑切「靈棋」分頁，展開幾張卡片看象曰／詩曰／又曰有沒有方框；125 卦目原典裡的罕用字最多，是最容易漏收的一批 | 同上。Session 46 就在靈棋文案上抓到過子集缺 4 字——那次是靠測試，但**測試只保證「掃到的字都收了」，收進去的字形在真機上長什麼樣仍要用眼睛看** |
| 5 | **觸覺回饋** | 抽棋、落子、收藏時手機應有震動 | `expo-haptics` 在 Web 是 no-op |
| 6 | **音效** | 抽棋、落子、揭曉、收藏、**靈棋擲卦（搖筒聲）**五種都要出聲；靜音鍵開啟時 iOS 的行為。設定頁關掉音效後五種都應完全靜默 | `sound.web.ts` 有分身，原生走 `expo-audio` 播 WAV。靈棋那一條是 Session 49 才接上的（在此之前靈棋全程無聲），而**原生播的是離線算好的檔案，不是即時合成**——音色對不對只有真機聽得出來 |
| 7 | **本地排程通知** | 設定開「每日提醒」→ 接受權限 → 確認排程成功；占卜後 14 天的占驗提醒可先把裝置日期往後調來驗 | 本地通知在 Expo Go 可用（SDK 57 只有 Android 推播不可用，本 App 不用推播） |
| 8 | **通知點擊導頁** | 點占驗提醒應進入 `/stats`，而非只是打開首頁 | 冷啟動路徑（`getLastNotificationResponseAsync`）只有真機殺掉 App 後才走得到 |
| 9 | **減少動態效果** | 系統開啟「減少動態」後再抽棋，墨滴轉場應跳過且**不得殘留遮罩** | 系統層設定，Web 的 `prefers-reduced-motion` 是另一套 |
| 10 | **安全區與瀏海** | 首頁、揭曉頁、棋盤頁的頂部不被瀏海/動態島遮住；底部導覽不被 home indicator 蓋住 | 模擬器與瀏覽器都不會重現真機的安全區 |
| 11 | **橫向與旋轉** | 旋轉後版面重算（棋盤格子、卡片寬度），不錯位 | `useViewport` 在原生走 RN 的 `useWindowDimensions`，與 Web 分支不同 |
| 12 | **雙機雲端同步** | A 機起卦 → B 機輸入相同配對碼 → 歷史／收藏／資料夾／自訂類別皆出現 | 需要兩個真實裝置與真實網路 |
| 13 | **拖曳落子** | 棋盤頁**不要先點**，直接按住棋子池的一顆棋拖到棋盤上放開，**棋子應落在放手的那一格**；再試「先點 A 後拖 B」，落下的必須是 B | Web 已由真 Chromium e2e 確認（document-level pointer 監聽 + 落子帶上被拖的棋子）。原生仍走既有 PanResponder，只有真機能確認兩端手勢責任是否一致。「先點再拖」會讓壞掉的情況也看似正常，故務必先試不點的走法 |
| 14 | **平板／橫向的棋子池側置** | 平板或手機橫放時，棋子池應出現在棋盤**右側**且與棋盤同一視線高度，提示文字寫「先從右側棋子庫…」 | 門檻吃的是量測到的容器寬（`computeBoardTray`），原生走 RN 的量測路徑，與 Web 的 `window.innerWidth` 分支不同 |
| 15 | **靈棋分享圖片有內容** | 靈棋頁按分享 → 打開那張圖，確認看得到卦名、卦目標記與象曰／詩曰 | 與 #1 同一條 `view-shot` 路徑，但**靈棋卡的版式不同**（沒有吉凶等級標籤、沒有棋子列），位元組數與籤詩卡不在同一個量級——#2 的 4KB 門檻要對這張卡另外驗一次，不能拿籤詩卡的結果推論 |
| 16 | **兩軍對壘陣的半場落子** | 棋盤頁選「兩軍對壘陣」，在紅方半場（下半）連放三子後，**該半場不該再出現落子點**，但黑方半場仍可放；點擊與拖曳兩種放法都試 | 半場上限由 `formationHalfFull` 同時擋住點擊與拖曳兩條路徑，而原生的拖曳走 PanResponder、Web 走 document-level pointer——**兩端是不同的手勢實作，Web 過不代表原生過**（#13 是同一個理由） |
| 17 | **AI 深度解讀在原生真的叫得到** | 連手機網路（不是電腦的區網）抽一次棋，揭曉頁的 AI 解讀應在 30 秒內回文字；關掉網路再試一次，應退回規則式解讀且不當機 | 端點位址在原生與 Web 是**不同分支**：Web 用相對路徑 `/api/interpret`，原生必須用絕對網址（`aiInterpretation.ts` 的 `ENDPOINT`）。相對路徑在原生 fetch 是直接失敗的，而失敗會被歸進「網路錯誤」，看起來像是使用者網路不好 |
| 18 | **占驗提醒的標題是當下介面語言** | 設定切 en 或 ja 後占一次卦，把裝置日期往後調 14 天，看通知內文的籤名是**譯文**而非中文原題；再用一筆靈棋記錄驗一次，應印卦名（如「大通卦」）而不是「龍騰九霄」 | 通知內文是排程當下就烘好的字串，Web 端根本沒有這條路徑（`Platform.OS === 'web'` 直接早退）。Session 49 才把它從 `poemTitle` 改成 `recordTitle()` |
| 19 | **靈棋擲完，連續天數真的往前走** | 連續兩天只擲靈棋（不抽棋不擺盤），第二天首頁應顯示「連續 2 天」；成就頁的「靈棋初擲」應在**擲完當下**就解開，不必先打開成就頁 | 這條在 Web 有 e2e 釘住，列在這裡是因為它牽涉 AsyncStorage 的真實寫入時序——原生的儲存後端與 Web 的 localStorage 不同。Session 49 修的正是「擲完什麼都沒記下」 |
| 20 | **快速提問範本的三層介面** | 抽棋／棋盤／靈棋任一頁：面向格應排成 3 欄、點「事業」後應出現情境 chip（求職面試／升遷轉職／職場關係／創業合作）、「換一批範本」要真的換掉 4 條、點範本後上方問題欄應填入該句且可再點其他範本替換 | RN 的 flex-wrap＋gap 在原生走 Yoga、在 Web 走 RNW 的 CSS flexbox——**兩套排版引擎**，Web 過不代表原生過（#13／#16 是同一個分支理由）。Session 50 才把三個占卜頁各自的類別列統一成這套元件，在此之前這塊根本不在清單上 |

### Expo Go 驗不到（必須等 preview binary）

- **App 圖示與啟動畫面**：Expo Go 顯示的是它自己的圖示
- **Android 推播**：SDK 53 起 Expo Go 不支援（本 App 只用本地通知，不受影響）
- **任何 config plugin 的效果**：`app.json` 的原生設定要重新 build 才生效
- **正式簽章、深連結、分享目標**：都屬 build-time 設定

---

## 第二階段：preview binary（上架前）

```sh
eas build --platform android --profile preview
eas build --platform ios --profile preview
```

在真機（至少一台 Android 13+ 與一台 iPhone）完成下列檢查：

1. 首次啟動、深色／淺色／系統主題，以及安全區與橫向縮放。
2. 抽棋、棋盤拖曳、撤銷、揭露動畫；**六種牌陣各起一次卦**（含兩軍對壘陣的
   半場限三子）與**靈棋擲卦**；開啟「減少動態效果」後不得殘留遮罩。
3. 音效、觸覺回饋與圖片分享；**籤詩卡與靈棋卡兩種版式各分享一次**，
   檢查分享圖上的繁中、英文、日文日期與牌陣名。
4. 開啟每日提醒，確認 iOS／Android 權限提示、上午 9 點排程與點擊通知後可進入 App。
5. 於兩台裝置完成雲端同步：第一台起卦、第二台輸入相同配對碼、確認歷史／收藏／資料夾／自訂類別皆出現。
6. 斷網、拒絕通知權限、關閉 AI 金鑰及 AI 限流時，主流程皆需保留規則式解讀且不能當機。
7. App 圖示、啟動畫面、深連結與分享目標（這幾項 Expo Go 驗不到）。

`expo-notifications` 的設定屬 native build-time config；改動 app.json 後必須重新建立 binary，不能只靠 Expo Go 驗證。
