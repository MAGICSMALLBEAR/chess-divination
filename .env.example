# 象棋占卜 環境變數設定範例
# 複製此檔案為 .env 並填入實際值

# DeepSeek AI API（用於 AI 深度解讀）
#
# 啟用方式：在 Vercel 專案設定中加入下列環境變數即可，不需改動程式碼。
#
# 端點是根目錄的 api/interpret.ts，Vercel 會自動偵測 api/ 目錄下的
# serverless function，與 Expo 的靜態網站產出（dist/）並存。
# vercel.json 的 rewrite 已排除 /api，不會把請求導向 index.html。
#
# 註：src/app/api/interpret+api.ts 是 Expo Router 版本的同一個端點，
# 只有在 app.json 的 web.output 設為 "server" 時才會匯出（目前是 "static"）。
# 兩者共用 src/services/aiPrompt.ts 的邏輯。實測過改成 "server" 會讓
# 產出結構變成 dist/client + dist/server，現有 vercel.json 失效，
# 且 expo serve 跑不起來（78 個 E2E 會全掛），故維持 "static"。
#
# 未設金鑰時端點回 501，前端顯示「AI 解讀尚未啟用」並保留規則式解讀。

DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# 雲端同步（預設使用本專案的 Vercel /api/sync）
# 在 Vercel Marketplace 連接 Upstash Redis 後，將這兩項環境變數設在 Vercel 專案中。
# 配對碼由 App 首次同步時生成，並由使用者在另一台裝置的「設定 → 裝置配對碼」輸入。
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 若要改用自己的同步伺服器才設定；伺服器必須接受 GET/PUT 和 X-Sync-Key header。
# EXPO_PUBLIC_CLOUD_SYNC_URL=
