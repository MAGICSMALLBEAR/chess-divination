# 象棋占卜 環境變數設定範例
# 複製此檔案為 .env 並填入實際值

# DeepSeek AI API（用於 AI 深度解讀）
#
# 注意：光設金鑰還不夠。Expo Router 的 API Route 只有在
# app.json 的 web.output 設為 "server" 時才會被匯出；
# 目前是 "static"，端點不存在，前端會顯示「AI 解讀尚未啟用」
# 並保留規則式深度解讀。改成 "server" 會改變 Vercel 的部署方式
# （從純靜態變成需要 serverless functions），故未擅自變更。
DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# 雲端同步伺服器（自建後端端點）
EXPO_PUBLIC_CLOUD_SYNC_URL=
