# 象棋占卜 環境變數設定範例
# 複製此檔案為 .env 並填入實際值

# DeepSeek AI API（用於 AI 深度解讀）
#
# 注意：光設金鑰還不夠。啟用 AI 解讀需要換部署平台，說明如下。
#
# Expo Router 的 API Route 只有在 app.json 的 web.output 設為
# "server" 時才會被匯出。目前是 "static"，端點不存在，
# 前端會顯示「AI 解讀尚未啟用」並保留規則式深度解讀。
#
# 已實測改成 "server" 的後果（2026-08-08）：
#   - API Route 確實會被匯出（/api/interpret）
#   - 但產出結構從扁平的 dist/*.html 變成 dist/client + dist/server，
#     現有 vercel.json（outputDirectory: dist + 全部 rewrite 到
#     index.html）會失效
#   - npx expo serve 不支援 server output（首頁也 404），
#     依賴它的 78 個 E2E 測試會全部跑不起來
#
# Expo 官方文件未提供 Vercel + server output 的部署指引，
# 建議改用 EAS Hosting（付費服務）。因此這不是改一行設定的事，
# 而是換部署平台的決定，故維持 "static"。
#
# 若日後要啟用，路徑是：
#   1. 改用 EAS Hosting，或自行為 Vercel 寫 Node adapter
#   2. app.json 的 web.output 改為 "server"
#   3. 設定下方的 DEEPSEEK_API_KEY
#   4. E2E 的 webServer 指令需一併改為新的啟動方式
DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# 雲端同步伺服器（自建後端端點）
EXPO_PUBLIC_CLOUD_SYNC_URL=
